apiVersion: v1
kind: Service
metadata:
  name: fundamentos-devops-service
spec:
  selector:
    app: fundamentos-devops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
